import React from "react";
import { useGameContext } from '../utils/GlobalState'

const Object = () => {
    const [state] = useGameContext();

    return (
        <div>
            
        </div>
    )
}